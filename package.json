{
  "name": "odyssey-voyage-airlock",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:CSalih/odyssey-voyage-airlock.git",
  "author": "Salih Candir <salih.candir@styleflasher.at>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "env-cmd lerna run start --stream --parallel",
    "db:reset": "lerna run db:reset",
    "db:init": "lerna run db:init",
    "subgraph:publish": "env-cmd lerna run subgraph:publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@apollo/rover": "^0.9.0",
    "lerna": "^5.5.4",
    "env-cmd": "^10.1.0"
  }
}
